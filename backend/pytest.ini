[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: Tests unitarios puros sin BD
    integration: Tests que requieren BD PostgreSQL de tests
    contract: Tests del contrato que documentan comportamiento esperado (pueden fallar)
